{
  "name": "spellbook",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "workspaces": [
    "apps/web",
    "apps/api",
    "apps/extension"
  ],
  "scripts": {
    "dev": "npm run dev:web",
    "dev:web": "npm run dev --prefix apps/web",
    "dev:api": "npm run dev --prefix apps/api",
    "test": "npm run test:web && npm run test:api && npm run test:extension && npm run test:docs",
    "test:web": "npm run test --prefix apps/web",
    "test:api": "npm run test --prefix apps/api",
    "test:extension": "npm run test --prefix apps/extension",
    "test:docs": "node scripts/check-docs.mjs",
    "build": "npm run build:web",
    "build:web": "npm run build --prefix apps/web",
    "build:pages": "node scripts/build-pages.mjs",
    "build:render": "npm run build:web",
    "snapshot:import": "node scripts/import-spells-csv.mjs"
  },
  "engines": {
    "node": ">=20"
  }
}
