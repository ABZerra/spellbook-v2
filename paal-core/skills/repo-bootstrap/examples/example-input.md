# Example Input: Repo Bootstrap

Need a repo setup plan for a new subscription product.

Answers to the bootstrap questions:
- Project type: mixed product
- Expected parts now: UI, backend, shared logic
- Reuse across parts: yes
- V1 priority: balanced
- Verification strictness: balanced

Known context:
- The web app and backend will both use the same pricing and entitlement rules.
- The team wants AI coding help without mixing UI styling rules into backend decisions.
- I do not have a fixed stack yet, but I want a sensible default recommendation for a small team.
